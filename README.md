# Chess Queen Coverage
A program to work out the least number of queens to place on a chess board of size n x m so that a queen can move to each square.

Requires Python 2

Execute the program by running `python qc_tester.py`.
